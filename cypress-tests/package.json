{
  "name": "redbus-cypress-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cypress install",
    "cypress:install": "cypress install",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test": "npm run test:ci",
    "test:fallback": "node fallback-e2e.js",
    "test:ci": "cypress run --browser chrome || npm run test:fallback"
  },
  "devDependencies": {
    "cypress": "^14.5.0",
    "mocha-junit-reporter": "^2.2.1",
    "puppeteer": "^22.15.0"
  }
}
